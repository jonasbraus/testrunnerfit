import 'package:flutter/material.dart';

class PageCalories extends StatelessWidget {
  const PageCalories({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
